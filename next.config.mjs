/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add remote hosts here if you serve art from a CDN (e.g. Cloudinary).
    // remotePatterns: [{ protocol: "https", hostname: "res.cloudinary.com" }],
  },
};

export default nextConfig;
